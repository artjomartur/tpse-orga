# NextAuth — zwingend für stabile Sessions (sonst „Abmeldung“ beim Seitenwechsel)
# Generieren z. B.: openssl rand -base64 32
NEXTAUTH_SECRET=

# Basis-URL der App (lokal)
NEXTAUTH_URL=http://localhost:3000

# Admin für prisma/seed.js
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=Admin
